# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Wxopen API
    version: 0.0.1
paths:
    /GetAccessToken:
        post:
            tags:
                - Wxopen
            operationId: Wxopen_GetAccessToken
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GetAccessTokenRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetAccessTokenResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /GetWxUserInfoByCode:
        post:
            tags:
                - Wxopen
            operationId: Wxopen_GetWxUserInfoByCode
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GetWxUserInfoByCodeRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetWxUserInfoByCodeResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /LoginQrCodeCreate:
        post:
            tags:
                - Wxopen
            operationId: Wxopen_LoginQrCodeCreate
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/LoginQrCodeCreateRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/LoginQrCodeCreateResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /LoginUrlCreate:
        post:
            tags:
                - Wxopen
            operationId: Wxopen_LoginUrlCreate
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/LoginUrlCreateRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/LoginUrlCreateResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        GetAccessTokenRequest:
            type: object
            properties:
                appId:
                    type: string
                appSecret:
                    type: string
        GetAccessTokenResponse:
            type: object
            properties:
                accessToken:
                    type: string
                expiresIn:
                    type: string
        GetWxUserInfoByCodeRequest:
            type: object
            properties:
                code:
                    type: string
        GetWxUserInfoByCodeResponse:
            type: object
            properties:
                openid:
                    type: string
                nickname:
                    type: string
                headimgurl:
                    type: string
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        LoginQrCodeCreateRequest:
            type: object
            properties:
                expireSeconds:
                    type: string
                sceneStr:
                    type: string
        LoginQrCodeCreateResponse:
            type: object
            properties:
                ticket:
                    type: string
                expireSeconds:
                    type: string
                url:
                    type: string
        LoginUrlCreateRequest:
            type: object
            properties:
                queryString:
                    type: string
        LoginUrlCreateResponse:
            type: object
            properties:
                url:
                    type: string
                state:
                    type: string
                appId:
                    type: string
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
tags:
    - name: Wxopen
